# Terraform state — never commit
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Sensitive var files — use SOPS-encrypted versions instead
*.auto.tfvars
secrets.tfvars
