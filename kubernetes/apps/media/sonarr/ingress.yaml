---
# UI (gated) — everything except the open API paths below requires an Authentik
# session via the shared domain-wide forward-auth middleware. Traefik routes the
# more specific /api, /feed, /ping prefixes (sonarr-api) ahead of this "/" router.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sonarr
  namespace: media
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    # Bouncer first (cheap IP-reputation check), then Authentik forward-auth.
    traefik.ingress.kubernetes.io/router.middlewares: traefik-security-headers@kubernetescrd,traefik-rate-limit@kubernetescrd,crowdsec-crowdsec-bouncer@kubernetescrd,authentik-authentik-forwardauth@kubernetescrd
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - sonarr.int.nerdbox.dev
        - sonarr.nerdbox.dev
      secretName: sonarr-tls
  rules:
    - host: sonarr.int.nerdbox.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sonarr
                port:
                  number: 8989
    - host: sonarr.nerdbox.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sonarr
                port:
                  number: 8989
---
# API (open) — /api, /feed (calendar/RSS) and /ping stay reachable with the app's
# own API key so Prowlarr/Jellyseerr/Bazarr/Recyclarr and monitoring keep working.
# No forward-auth middleware here. Reuses the sonarr-tls cert minted by the UI
# Ingress above (no cert-manager annotation -> no duplicate Certificate).
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sonarr-api
  namespace: media
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    # CrowdSec IP-reputation bouncer in front of the open (unauthenticated) API paths.
    traefik.ingress.kubernetes.io/router.middlewares: traefik-security-headers@kubernetescrd,traefik-rate-limit@kubernetescrd,crowdsec-crowdsec-bouncer@kubernetescrd
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - sonarr.int.nerdbox.dev
        - sonarr.nerdbox.dev
      secretName: sonarr-tls
  rules:
    - host: sonarr.int.nerdbox.dev
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: sonarr
                port:
                  number: 8989
          - path: /feed
            pathType: Prefix
            backend:
              service:
                name: sonarr
                port:
                  number: 8989
          - path: /ping
            pathType: Prefix
            backend:
              service:
                name: sonarr
                port:
                  number: 8989
    - host: sonarr.nerdbox.dev
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: sonarr
                port:
                  number: 8989
          - path: /feed
            pathType: Prefix
            backend:
              service:
                name: sonarr
                port:
                  number: 8989
          - path: /ping
            pathType: Prefix
            backend:
              service:
                name: sonarr
                port:
                  number: 8989
