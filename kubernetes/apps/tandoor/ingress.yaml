---
# Internal-only. No forward-auth middleware — Tandoor has its own login + OIDC
# (Authentik via django-allauth), and a domain-wide forward-auth would break the
# OIDC callback and the REST API. SSO is handled inside Tandoor.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tandoor
  namespace: tandoor
  annotations:
    # CNAME to the single self-healing ingress record (see the Traefik Service)
    # rather than a second A record, so a Traefik IP change moves one record.
    external-dns.alpha.kubernetes.io/target: ingress.int.nerdbox.dev
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.middlewares: traefik-security-headers@kubernetescrd,traefik-internal-only@kubernetescrd
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - tandoor.int.nerdbox.dev
      secretName: tandoor-tls
  rules:
    - host: tandoor.int.nerdbox.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tandoor
                port:
                  number: 8080
