---
# Paperless uses Redis purely as the Celery broker / result cache — its contents
# are reconstructable (the consume dir is re-scanned and the index is rebuildable),
# so it is intentionally NOT persisted or backed up. Same pattern as
# immich/redis.yaml, but with an emptyDir since nothing here is worth keeping.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: paperless-redis
  namespace: paperless
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: paperless-redis
  template:
    metadata:
      labels:
        app: paperless-redis
    spec:
      securityContext:
        runAsUser: 999
        runAsGroup: 999
        fsGroup: 999
      containers:
        - name: redis
          image: redis:8.10.0-alpine
          args:
            - --save
            - ""           # disable RDB snapshots
            - --appendonly
            - "no"         # pure broker/cache; not persisted/backed up
          ports:
            - name: redis
              containerPort: 6379
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              memory: 256Mi
        # Metrics sidecar — same pattern as immich/redis.yaml, which carries the full
        # rationale. localhost to Redis, no credentials (no requirepass here).
        - name: redis-exporter
          image: oliver006/redis_exporter:v1.89.0-alpine@sha256:d4e0a0ad55fa4f968eea4519b518b16dac887debd06c790a6be2f60940538d82
          env:
            - name: REDIS_ADDR
              value: redis://localhost:6379
          ports:
            - name: metrics
              containerPort: 9121
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              memory: 64Mi
      volumes:
        - name: data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: paperless-redis
  namespace: paperless
  # Labelled so the ServiceMonitor below can select it.
  labels:
    app: paperless-redis
spec:
  selector:
    app: paperless-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
    - name: metrics
      port: 9121
      targetPort: 9121
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: paperless-redis
  namespace: paperless
spec:
  selector:
    matchLabels:
      app: paperless-redis
  endpoints:
    - port: metrics
      interval: 30s
      scrapeTimeout: 10s
