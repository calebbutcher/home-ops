---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# No namespace.yaml — this deploys into kube-system, alongside the CoreDNS
# Deployment it scales.
resources:
  - helmrepository.yaml
  - helmrelease.yaml
